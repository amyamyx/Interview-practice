// Write a function, fibs(num) which returns the first n elements 
// from the fibonnacci sequence, given n.

// Solve it both iteratively and recursively.

function fibs(num) {

}

function fibs_rec(num) {

}